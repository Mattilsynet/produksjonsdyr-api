title: Driftsperiode
type: object
description: 'Angir om det er kontinuerlig drift hele året. Hvis ikke, angis start og stop måned. Hvor måned angis i månedsnummer.'
properties:
  driftsperiodeStartmaaned:
    type: integer
    format: int32
  driftsperiodeStopmaaned:
    type: integer
    format: int32
  kontinuerligDrift:
    type: boolean
required:
  - kontinuerligDrift
